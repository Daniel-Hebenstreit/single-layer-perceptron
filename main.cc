#include "lib.h"
#include <iostream>

int main()
{

    // Eingabewerte. Dritter Wert ist jeweils korrekter bzw. erwarter Wert
    //double matrix[4][3] = {{2, 5, 1}, {5, 4, 0}, {3, 3, 0}, {3, 6, 1}};

    update_weights();

    return 0;
}
